---
name: bph-preference-counting
description: 从褐飞虱间隔摄影或已有逐图计数生成虫子数量随时间变化的曲线图。用于取食偏好照片分析与曲线复绘，交付带时间衔接标识的中文PNG、SVG和PDF；逐图计数、校准核验与时间统计服务于最终出图，候选计数保留待复核标识。
metadata:
  short-description: 从逐张照片生成带衔接标识的褐飞虱数量曲线图
---

# 褐飞虱数量曲线图

**最终目的：输出用户能够直接查看和使用的“虫子数量随时间变化曲线图”。** 逐图计数、校准核验、时间整理和统计是完成这张图的步骤。接到照片分析请求后，推进到曲线生成、视觉检查和交付；清单、模板、计数表或测试记录本身不代表这项任务已完成。用中文沟通，保留原图，结果写入新目录。

## 最终曲线图的交付要求

- 本案例按三个实验分面，每个分面画左侧对照与右侧抗性两条曲线，统一蓝色与橙色。新实验按当前已确认的分组绘线。
- 横轴为“累计有效观察时间（小时）”；纵轴为“株上可见虫数（只）”，默认绘制每5分钟的时间加权平均数。保留全部有效时长。
- 停拍缺口在有效时间轴上压缩，前后曲线连续衔接，横坐标显示短刻线和“衔接”文字；连接线不增加停拍期间的虫数或观察时长。
- 首要交付为实际生成的中文曲线PNG（300 dpi）、可编辑SVG和矢量PDF。打开实际PNG检查后，在回复中展示曲线，并给出SVG/PDF链接。
- 真实时间缺口曲线、逐小时热图、Excel/CSV和追溯报告作为随附材料，支持主曲线的理解和复现。
- 未通过计数核验时，将已有逐图自动候选生成清楚标注“自动候选计数，待复核”的曲线，并保留已达初检范围的主统计图。无法计数的数据保持为空；缺少原图或计数证据时说明实际阻塞，不为出图编造数值。曲线已交付与计数验收已通过分别说明。

## 选择执行模式

| 请求 | 操作与边界 |
|---|---|
| 重画本次数量曲线 | `replot`：使用包内26,403行历史逐图数据直接出图，不需要原图；不是再次检测或新的目视验收。 |
| 从本次原始照片完整重跑并出图 | `prepare → smoke → run → export`：使用冻结脚本、背景及权重；逐图核对原图SHA256和计数，一致后才能继承历史核验状态，最后导出曲线。 |
| 新照片、新机位或新材料画面 | 阅读[新数据与计数约定](references/analysis-contract.md)。`init-new`是起点，随后继续清点、校准、适配、逐张计数、核验和统计，直至输出曲线；冻结检测器不能直接用于新场景。 |

先读[复现命令与运行要求](references/reproduction.md)。需要了解本次参数、结果状态或失败原因时，再读[冻结案例](references/frozen-case.md)。数据格式见[字段与追溯](references/schema.md)。

## 不可省略的计数与时间规则

- 当前会话已确认的左右分组和统计范围优先。本次为左对照、右抗性；A/B各侧5株、C各侧4株，输出侧合计。新实验不能照搬位置与株数。
- 每张原图独立产生候选和计数。邻帧只能辅助核对；不能以抽样、邻帧复制或插值填满未处理照片。长期静止虫体仍是目标；单用运动差分或时间背景扣除会漏计。
- 主计数只包括能够归属到植株茎叶的可见褐飞虱；箱壁、底部、飞行、倒影与归属不明分开。疑似标记可与左右候选重叠，不能相加当总虫数。无法可靠计数为NA，不是0。
- 校准与留出照片分开。默认沿用每组20张校准、30张初始留出；调整后记录实际设计。每侧MAE≤1只且至少95%的误差≤2只，是本次初始目标，不是普适精度保证。参考缺失、重叠无法分辨或未达目标时保留失败状态；继续适配后须用新的留出样本验收。
- 模型目视参考、实验人员目视参考、自动检测和逐图人工修正分别记来源。不能把模型自检改称独立人工专家验收，也不能把技术完整性检查当作计数精度合格。
- 以实际拍摄时间排序。只积分相邻两帧均有效且 `0 < Δt ≤ 15秒` 的区间；跨停拍、无效帧和重复时间不补值。保持真实经过时间、累计有效时间及对应表。
- 有效时间主图去除缺口并连接曲线，横坐标用短刻线和“衔接”标识；连接不增加观察时长或虫数。真实时间附图按连续片段分开绘线。
- 5分钟均值为虫数×时间积分除以纳入秒数；逐小时占比为各侧积分除以两侧积分合计。零分母占比留空，保留最后不足一小时的窗口，不统一截到24小时。
- 每个日期目录按已确认口径是一场连续观察。连续照片不是独立实验重复；可见停留分布不等于实际吸食量。

## 执行与交付

1. 检查现有Python环境、原图挂载、输入目录、重复与时间字段。`doctor`只检查，不安装依赖或联网。原图复现写到新的输出目录；已有无关目录会被拒绝。
2. 先做小批量检查。冻结案例的 `smoke` 独立检测7张原图，覆盖三组及回补组失焦样本，核对SHA256和逐侧候选数；这验证代码复现，不能替代新准确性核验。
3. 原图全量处理用 `run`，保留逐图JSONL断点。参数、脚本或模型改变时另建版本。原图或计数与冻结案例不匹配时停止继承旧验收状态，保留差异报告。
4. 全量完整后 `export` 生成虫子数量曲线图，同时保存Excel/CSV、逐小时热图、坐标、样本及异常帧标注、报告和本机复核入口。历史复绘用 `replot` 直接完成出图。图形输出SVG、PDF与300 dpi PNG。
5. 检查全量记录、坐标/计数一致、缺口未积分、时间窗积分守恒、Excel/CSV一致、SVG曲线连接和中文标签。打开实际曲线PNG做视觉检查，并把成图交付用户。若测试只覆盖小批量或统计复绘，报告这个范围。

本次冻结结果仅A组达到初始模型目视核验目标，B/C未通过；复现成功不得把它们改成通过。静止虫体、密集重叠等未消除的问题见[冻结案例](references/frozen-case.md)。

结束时首先展示虫子数量曲线图及其文件链接，再给出随附表格与报告，简述图形覆盖范围及尚未验收的计数。任务完成的必要条件是曲线已实际生成、检查并交付；若仍有真实阻塞，明确未完成的部分。不要仅因输出文件生成成功就宣称科研计数全部完成。

本Skill已执行的验证及其范围见[验证记录](references/validation.md)；可重复检查的入口见[复现命令](references/reproduction.md)。
